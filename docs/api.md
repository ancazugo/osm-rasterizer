# Python API

The public API is exposed at the top level of the `osm_rasterizer` package:

```python
from osm_rasterizer import rasterize, RasterizeResult, fetch_features, get_utm_crs
```

## rasterize

::: osm_rasterizer.rasterize

## RasterizeResult

::: osm_rasterizer.RasterizeResult

## fetch_features

::: osm_rasterizer.fetch_features

## get_utm_crs

::: osm_rasterizer.get_utm_crs
